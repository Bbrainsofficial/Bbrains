{
  "name": "@bbrains/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and interfaces for BBrains.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0",
    "test": "exit 0",
    "clean": "rimraf dist node_modules"
  },
  "devDependencies": {
    "eslint": "^9.18.0",
    "typescript": "^5.7.3"
  }
}