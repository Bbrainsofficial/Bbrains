{
  "name": "@bbrains/validators",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod validation schemas for BBrains API contracts (shared between web, mobile, and api).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0",
    "test": "vitest run",
    "clean": "rimraf dist node_modules"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "eslint": "^9.18.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}